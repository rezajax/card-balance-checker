# 📁 ساختار پروژه Browser Automation

```
cursor/
│
├── 📄 browser_automation.py          ⭐ نسخه ساده - شروع از اینجا!
│   └── کلاس BrowserAutomation با قابلیت‌های پایه
│
├── 📄 advanced_automation.py         ⭐ نسخه پیشرفته - برای کارهای حرفه‌ای
│   └── کلاس AdvancedBrowserAutomation با:
│       ├── Retry mechanism
│       ├── Smart selectors  
│       ├── Logging حرفه‌ای
│       ├── Error handling
│       └── چند مرورگر (chromium, firefox, webkit)
│
├── 📄 example_usage.py               ⭐ 10 مثال کاربردی
│   ├── فرم‌ها و input ها
│   ├── استخراج داده
│   ├── Dropdown و Checkbox
│   ├── Infinite Scroll
│   ├── File Upload
│   ├── Hover و Menu
│   └── Parallel browsers
│
├── 📄 test_installation.py           ⭐ تست نصب
│   └── چک کردن اینکه همه چیز کار میکنه
│
├── 🔧 setup.sh                       ⭐ نصب خودکار
│   └── همه چیز رو یکجا نصب میکنه
│
├── 📋 requirements.txt               Python dependencies
│   ├── playwright
│   ├── asyncio
│   ├── python-dotenv
│   └── aiofiles
│
├── ⚙️  .env.example                   نمونه تنظیمات
│   └── کپی کن به .env و ویرایش کن
│
├── 📖 README.md                      مستندات کامل (English)
│   ├── راهنمای نصب
│   ├── توضیح قابلیت‌ها
│   ├── چطور سلکتور پیدا کنیم
│   └── رفع مشکلات
│
└── 📖 QUICKSTART_FA.md               راهنمای سریع (فارسی)
    └── برای شروع سریع کار

```

## 🎯 چی از کجا استفاده کنم؟

### برای شروع سریع:
```bash
1. ./setup.sh              # نصب همه چیز
2. cp .env.example .env    # کپی تنظیمات
3. nano .env               # ویرایش تنظیمات
4. python advanced_automation.py
```

### برای یادگیری:
```bash
1. cat README.md           # خوندن مستندات
2. python example_usage.py # اجرای مثال‌ها
3. cat example_usage.py    # دیدن کدها
```

### برای تست:
```bash
python test_installation.py
```

### برای پروژه شخصی:

**سناریو 1: کار ساده** (یک صفحه، چند فیلد)
→ استفاده از `browser_automation.py`
→ فقط بخش CONFIG رو ویرایش کن

**سناریو 2: کار پیچیده** (چند صفحه، error handling، retry)
→ استفاده از `advanced_automation.py`
→ تنظیمات از طریق .env
→ یا import کن و customize کن:

```python
from advanced_automation import AdvancedBrowserAutomation

automation = AdvancedBrowserAutomation(headless=True)
# کد شما...
```

**سناریو 3: یادگیری کار با Playwright**
→ `example_usage.py` رو باز کن
→ کدهای مثال‌ها رو بخون
→ برای تست اجراش کن

## 🔑 فایل‌های کلیدی

| فایل | کاربرد | کِی استفاده کنم؟ |
|------|---------|------------------|
| `browser_automation.py` | اتومیشن ساده | برای کارهای ساده و یادگیری |
| `advanced_automation.py` | اتومیشن حرفه‌ای | برای production و کارهای پیچیده |
| `example_usage.py` | مثال‌ها | برای یادگیری Playwright |
| `test_installation.py` | تست | بعد از نصب برای چک کردن |
| `setup.sh` | نصب | یک بار در اول |
| `.env` | تنظیمات | برای advanced_automation |

## 📊 فایل‌های خروجی (بعد از اجرا)

```
cursor/
├── results.json              # نتایج استخراج شده
├── results_*.json            # نتایج با timestamp
├── screenshot_*.png          # اسکرین‌شات‌ها
├── error_screenshot.png      # اسکرین‌شات خطا
├── automation.log            # لاگ‌های اجرا
└── .env                      # تنظیمات شما (از .env.example)
```

## 🎓 مسیر یادگیری پیشنهادی

1. **نصب** → `./setup.sh`
2. **تست** → `python test_installation.py`
3. **یادگیری** → `python example_usage.py` (مثال 1 رو اجرا کن)
4. **تمرین** → کد `browser_automation.py` رو ویرایش و اجرا کن
5. **پیشرفته** → `advanced_automation.py` رو import کن و customize کن

## 💡 نکات مهم

✅ **همیشه اول `test_installation.py` رو اجرا کن**
✅ **برای debug، `headless=False` کن تا مرورگر رو ببینی**
✅ **لاگ‌های `automation.log` رو برای debug بخون**
✅ **از `example_usage.py` برای یادگیری استفاده کن**

## 🆘 کمک سریع

**مشکل: Playwright نصب نیست**
```bash
pip install playwright
python -m playwright install chromium
```

**مشکل: مرورگر باز نمیشه**
```bash
python -m playwright install-deps
```

**مشکل: سلکتور کار نمیکنه**
- Developer Tools → Inspect → Copy selector
- از XPath استفاده کن: `xpath=//button[text()="کلیک"]`

**مشکل: داره خطا میده**
- فایل `automation.log` رو باز کن
- `error_screenshot.png` رو نگاه کن
- با `headless=False` اجرا کن

---

**موفق باشی! 🚀**
