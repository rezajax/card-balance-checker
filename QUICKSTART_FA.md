# 🚀 راهنمای سریع شروع کار

## نصب (فقط یک بار)

```bash
# اجرای اسکریپت نصب خودکار
./setup.sh
```

یا به صورت دستی:

```bash
# نصب packages
pip install -r requirements.txt

# نصب مرورگرها
python -m playwright install chromium
```

## استفاده سریع

### گام 1️⃣: کپی و تنظیم فایل محیطی

```bash
cp .env.example .env
nano .env  # یا هر ویرایشگر دیگه
```

### گام 2️⃣: یکی از روش‌های زیر رو انتخاب کن

#### روش A: ویرایش مستقیم کد (ساده‌تر)

فایل `browser_automation.py` رو باز کن و بخش `CONFIG` رو ویرایش کن:

```python
CONFIG = {
    'url': 'https://YOUR-WEBSITE.com',
    'input_numbers': ['123', '456', '789'],
    'input_selectors': ['#field1', '#field2', '#field3'],
    'submit_selector': 'button[type="submit"]',
    'result_selectors': ['.result1', '.result2'],
}
```

بعد اجرا کن:

```bash
python browser_automation.py
```

#### روش B: استفاده از فایل .env (حرفه‌ای‌تر)

فایل `.env` رو ویرایش کن:

```env
WEBSITE_URL=https://YOUR-WEBSITE.com
INPUT_NUMBERS=123,456,789
INPUT_SELECTORS=#field1,#field2,#field3
SUBMIT_SELECTOR=button[type="submit"]
RESULT_SELECTOR_1=.result1
RESULT_SELECTOR_2=.result2
```

بعد اجرا کن:

```bash
python advanced_automation.py
```

## 🎯 پیدا کردن سلکتورها

1. سایت رو با مرورگر عادی باز کن
2. `F12` رو بزن (Developer Tools)
3. روی آیکون Inspect کلیک کن (گوشه بالا سمت چپ)
4. روی المنت مورد نظر کلیک کن
5. در Developer Tools، روی HTML راست کلیک کن
6. `Copy > Copy selector` رو انتخاب کن

## 📝 مثال واقعی

فرض کن یک ماشین‌حساب آنلاین داریم:

**سایت:** `https://calculator.com`

**فیلدها:**
- فیلد اول: `#number1`
- فیلد دوم: `#number2`  
- دکمه محاسبه: `button.calculate`
- نتیجه: `#result`

**کد:**

```python
CONFIG = {
    'url': 'https://calculator.com',
    'input_numbers': ['10', '20'],
    'input_selectors': ['#number1', '#number2'],
    'submit_selector': 'button.calculate',
    'result_selectors': ['#result'],
}
```

## 🛠️ تست کردن

برای تست، میتونی از Google استفاده کنی:

```bash
python example_usage.py
```

بعد شماره `1` رو انتخاب کن تا یک جستجوی ساده انجام بده.

## 📊 خروجی‌ها

بعد از اجرا، این فایل‌ها ساخته میشن:

- ✅ `results.json` - نتایج JSON
- ✅ `screenshot_*.png` - عکس از صفحه
- ✅ `automation.log` - لاگ‌ها

## ❓ مشکلات رایج

### مرورگر باز نمیشه؟

```bash
# مطمئن شو که مرورگرها نصب شدن
python -m playwright install chromium
```

### المنت پیدا نمیشه؟

- سلکتور رو دوباره چک کن
- `headless=False` کن تا ببینی چی میشه
- تایم‌اوت رو بیشتر کن

### سایت تشخیص میده که بات هست؟

کد ما قبلا این مشکل رو حل کرده! ولی میتونی:
- `slow_mo` رو بیشتر کنی
- بین عملیات `sleep` اضافه کنی

## 🎓 یادگیری بیشتر

مثال‌های بیشتر:

```bash
python example_usage.py
```

این فایل 10 تا مثال مختلف داره:
- فرم‌ها
- Dropdown و Checkbox
- Infinite Scroll
- آپلود فایل
- و خیلی بیشتر!

## 💡 نکته مهم

**همیشه احترام به قوانین سایت‌ها رو داشته باش!**

- `robots.txt` رو چک کن
- از scraping بیش از حد خودداری کن
- بین درخواست‌ها تاخیر بذار

---

**موفق باشی! 🚀**

سوال داری؟ فایل `README.md` رو بخون یا کد رو باز کن - همه چیز توضیح داده شده!
